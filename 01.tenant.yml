- name: Add a new Tenant
  hosts: all
  gather_facts: false

  tasks:
    - name: Add a new tenant
      validate_certs: false
      cisco.aci.aci_tenant:
        tenant: "{{ tenant }}"
        description: "" 
        state: "{{ state }}"
      delegate_to: localhost

