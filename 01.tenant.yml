- name: Add a new Tenant
  hosts: all
  connection: local
  gather_facts: false
  vars:
    ansible_connection: local
  tasks:
    - name: Add a new tenant
      cisco.aci.aci_tenant:
        hostname: "{{ hostname }}"
        use_proxy: "{{ use_proxy }}"
        use_ssl: "{{ use_ssl }}"
        username: "{{ username }}"
        password: "{{ password }}"
        validate_certs: no
        description: "" 
        state: "{{ state }}"
      delegate_to: localhost

